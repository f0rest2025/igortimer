[project]
name = "igortimer"
version = "0.1.0"
description = "Desktop billable time tracker with a floating timer window."
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.5.0",
    "openpyxl>=3.1.0",
    "pynput>=1.7.6",
    "darkdetect>=0.8.0",
]
